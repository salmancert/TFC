# Dependencies for the bank reconciliation tool only.
# (The travel-cost forecasting app in this repo needs the much heavier
#  requirements.txt; you do not need TensorFlow or Prophet to reconcile.)
joblib>=1.3
numpy>=1.24
openpyxl>=3.1
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10

# The desktop app also needs tkinter, which ships with the standard
# python.org installers. On Linux install it from your package manager:
#   Debian/Ubuntu: sudo apt install python3-tk
#   Fedora:        sudo dnf install python3-tkinter

# The sketch theme's handwriting fonts are bundled in
# bank_reconciliation/assets/fonts/ (SIL Open Font License) - nothing to install.
