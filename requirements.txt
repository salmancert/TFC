# Runtime dependencies. Deliberately small: the forecaster is pandas and numpy
# arithmetic, so there is no TensorFlow or Prophet to install, no GPU, and no
# multi-hundred-megabyte wheel. It installs and runs on an ordinary laptop.
Flask==3.1.2
pandas==2.3.3
numpy==2.3.5
openpyxl==3.1.5
requests==2.32.5

# Optional: only needed to serve the app to more than one user.
gunicorn==23.0.0
